name: seatrack
description: SeaTrack - Pencatat transaksi otomatis dari notifikasi SeaBank, DANA, dan GoPay.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.1
  notification_listener_service: ^1.0.0
  provider: ^6.1.2
  intl: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
